{
  "name": "hinos-framework",
  "version": "1.0.0",
  "description": "hinos project which is built for APIs",
  "main": ".build/index.js",
  "scripts": {
    "start": "node .",
    "preinstall": "npm install -g typescript",
    "build:clean": "rimraf .build .spec",
    "build": "npm run build:clean && tsc",
    "dev": "npm run build:clean && tsc --watch --pretty --sourceMap",
    "pretest": "rimraf .spec && tsc -p src/test/tsconfig.json",
    "test": "mocha --opts src/test/mocha.opts \".spec/test/spec/*.js\"",
    "pm2": "npm run build && pm2 start pm2.config.js"
  },
  "dependencies": {
    "hinos": "^1.0.6",
    "hinos-bodyparser": "^1.0.6",
    "hinos-common": "^1.0.4",
    "hinos-cors": "^1.0.0",
    "hinos-mongo": "^1.0.4",
    "hinos-rabbitmq": "^1.0.4",
    "hinos-redis": "^1.0.4",
    "hinos-requestmatcher": "^1.0.3",
    "hinos-route": "^1.0.6",
    "hinos-serve": "^1.0.6",
    "hinos-socketio": "^1.0.3",
    "hinos-validation": "^1.0.5",
    "lodash": "^4.17.4"
  },
  "author": "doanthuanthanh88@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^3.5.2",
    "@types/lodash": "^4.14.67",
    "@types/mocha": "^2.2.41",
    "@types/node": "^7.0.32",
    "chai": "^3.5.0",
    "mocha": "^3.3.0",
    "rimraf": "^2.6.1"
  }
}
