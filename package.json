{
  "name": "hinos-mail-service",
  "version": "1.0.0",
  "description": "hinos project which is built for APIs",
  "main": ".build/index.js",
  "scripts": {
    "start": "node .",
    "build": "rimraf .build && tsc",
    "pretest": "rimraf .spec && tsc -p src/test/tsconfig.json",
    "test": "mocha --opts src/test/mocha.opts \".spec/test/spec/*.js\"",
    "dev": "rimraf .build && tsc --watch --pretty --sourceMap",
    "pm2": "cross-var \"npm run build && pm2 start . --env production --name $npm_package_name\""
  },
  "config": {
    "externalConfig": "../config.cfg#mail",
    "url": "http://localhost:1003",
    "routes": [],
    "services": {
      "oauth": "http://localhost:1001"
    },
    "encryptKey": ")^!!!(**",
    "mongo": {
      "url": "mongodb://localhost:27017/mail",
      "releaseTimeout": 50000
    },
    "app": {
      "scanTimeout": 15000
    }
  },
  "dependencies": {
    "hinos": "^1.0.8",
    "hinos-bodyparser": "^1.1.1",
    "hinos-common": "^1.0.5",
    "hinos-cors": "^1.0.0",
    "hinos-mongo": "^1.0.7",
    "hinos-requestmatcher": "^1.0.5",
    "hinos-route": "^1.1.3",
    "hinos-validation": "^1.1.0",
    "lodash": "^4.17.4",
    "nodemailer": "^4.0.1"
  },
  "author": "doanthuanthanh88@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^3.5.2",
    "@types/lodash": "^4.14.70",
    "@types/mocha": "^2.2.41",
    "@types/node": "^7.0.38",
    "@types/nodemailer": "^1.3.33",
    "chai": "^3.5.0",
    "mocha": "^3.3.0",
    "rimraf": "^2.6.1"
  }
}
